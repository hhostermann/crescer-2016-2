import static org.junit.Assert.*; 
import org.junit.After; 
import org.junit.Before; 
import org.junit.Test; 
 
public class ElfoTest 
{ 
    @Test 
    public void elfoNasceComNome() { 
        // Arrange 
        String nomeEsperado = "Bruce Wayne"; 
        // Act 
        Elfo elfoDoTeste = new Elfo(nomeEsperado); 
        // Assert 
        assertEquals(nomeEsperado, elfoDoTeste.getNome()); 
    } 
    @Test 
    public void elfoNasceComArco() { 
        // Act 
        Elfo elfoDoTeste = new Elfo("Elrond"); 
        // Assert 
        assertEquals("Arco", elfoDoTeste.getArco().getDescricao()); 
        assertEquals(1, elfoDoTeste.getArco().getQuantidade()); 
    } 
    @Test
    public void elfoNasceComFlecha() {
        // Act 
        Elfo elfoDoTeste = new Elfo("Galandriel"); 
        // Assert 
        assertEquals("Flecha", elfoDoTeste.getFlecha().getDescricao()); 
        assertEquals(42, elfoDoTeste.getFlecha().getQuantidade()); 
    }
    @Test 
    public void elfoAtirarFlecha() { 
        // Arrange
        // Act
        Elfo elfoDoTeste = new Elfo ("Arwen");
        Dwarf dwarfDoTeste = new Dwarf("Aleatorio");
        elfoDoTeste.atirarFlecha();
        assertEquals(41, elfoDoTeste.getFlecha().getQuantidade());
    }
    /*@Test //não funcionando ainda
    public void elfoBullyn() { 
        // Arrange
        Elfo elfoDoTeste = new Elfo ("Elladan");
        Dwarf dwarfDoTeste = new Dwarf("Aleatorio");
        // Act
        elfoDoTeste.atirarFlecha();
        assertEquals(41, elfoDoTeste.getFlecha().getQuantidade());
        assertEquals(100, dwarfDoTeste.getPontosDeVida());
    }*/
    
}