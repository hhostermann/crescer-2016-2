import static org.junit.Assert.*; 
import org.junit.After; 
import org.junit.Before; 
import org.junit.Test; 
public class LembaTest
{ 
    @Test 
    public void divideMetade() { 
        CestaLemba cestaTeste = new CestaLemba(1);
        Item itemDoTeste = new Item("Lemba", 22); 
        assertEquals(true, cestaTeste.getLemba().getQuantidade()); 
    }
}
