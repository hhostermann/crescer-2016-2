class ClassName {
  constructor(array) {
    this.array = array;
  }
 foraDaGuerraCivil() {
    for(var i = 0 ; i < this.array.length ; i++){
      var objeto = this.array[i];
      var evento = objeto.events;
      var item = evento.items;
      var fora = false;
      for(var j=0;j<itens.length;j++){
      if(itens[j].name ==! 'Civil War'){
        fora = true;
      }
      }
      if(fora){
        console.log(objeto.name);
      }
    }
}
}
